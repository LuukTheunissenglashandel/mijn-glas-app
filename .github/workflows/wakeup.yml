name: Wake up App

on:
  schedule:
    - cron: '0 5 * * *' # 07:00 Amsterdamse tijd (zomer)
  workflow_dispatch:    # <--- DIT VOEGT DE TEST-KNOP TOE

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl -I "https://theunissen.streamlit.app/?wake=true"
